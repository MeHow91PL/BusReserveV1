import {connect} from 'react-redux';
import React from 'react';

// import { actionCreator } from '../actionPath'

const OknoOpcji = () => {
    return (
        <div className="oknoOpcji modalDialog">
            <div>
                Opcje
            </div>
        </div>
    )
}

// const mapStateToProps = (state, ownProps) => ({ }) const mapDispatchToProps =
// { }
export default OknoOpcji;
// export default connect(mapStateToProps, mapDispatchToProps)(OknoOpcji)